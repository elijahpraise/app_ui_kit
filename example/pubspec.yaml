name: app_ui_kit_example
description: "Example app for app_ui_kit."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.2
  flutter: ">=3.35.4"

dependencies:
  flutter:
    sdk: flutter
  app_ui_kit:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
